# Demos

Store preview assets under `demos/<skill-name>/`.

Recommended naming:

- `preview.png` or `preview.gif` for the main skill preview shown in the root README
- `01-overview.png`, `02-output.png` for additional screenshots

Use the real screenshot or a downsized `preview.png` in the root README once the demo is ready.
